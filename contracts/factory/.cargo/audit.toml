[advisories]
ignore = [
    # time >= 0.3.47 needs Rust 1.88 (we pin 1.85.0). Only reached through
    # near-sdk's unit-testing dependencies, never compiled into the contract wasm.
    "RUSTSEC-2026-0009",
]
